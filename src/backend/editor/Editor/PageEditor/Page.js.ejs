class <%=page%><%=_class%>Controller extends <%=_class%>Controller {

    /*
    constructor(...args) {
        super(...args);
    }
    */

    /*
    init() {
        super.init();
    }
    */

    /*
    deinit() {
        super.deinit();
    }
    */

    /*
    getTitle() {
        // const formCaption = this.getForm('formName').getTitle();
        return this.getModel().getCaption();
    }
    */

    /*
    getViewClass() {
        return <%=page%>PageView;
    }
    */

    /*
    async onAction1() {
    }
    async onActionClick(name) {
        switch (name) {
            case 'action1': await this.onAction1(); return true;
        }
        return false;
    }
    */

}
