const { Page, BaseModel } = require('../../../../../qforms');

class <%=name%>BackPage extends Page {
    constructor(...args) {
        console.log('<%=name%>BackPage.constructor');
        super(...args);
    }
    /*async getChildModelCustomClass(colName, data) {
        if (colName === 'forms') {
            if (BaseModel.getName(data) === 'Form1') {
                return Form1Form;
            }
        }
        return null;
    }*/
}
module.exports = <%=name%>BackPage;
