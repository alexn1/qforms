class <%=page%><%=form%>Controller extends <%=_class%>Controller {

    constructor(...args) {
        super(...args);
    }

    init() {
        super.init();
    }

    deinit() {
        super.deinit();
    }

    /*
    getCaption() {
        // this.row  // for RowForm
        return this.model.data.caption;
    }
    */

    /*
    setRowStyle(bodyRow, row) {
        //const fieldView = this.fields.field.views[bodyRow.qKey];
        //if (row.column === 'value') {
        //    $(fieldView).css('color', 'red');
        //} else {
        //    $(fieldView).css('color', '');
        //}
    }
    */

}
<%=page%><%=form%>Controller;
