class <%=page%><%=form%>Controller extends <%=_class%>Controller {

    /*
    constructor(...args) {
        super(...args);
    }
    */

    /*
    init() {
        super.init();
    }
    */

    /*
    deinit() {
        super.deinit();
    }
    */

    /*
    getViewClass() {
        // return TableFormView;
        // return RowFormView;
    }
    */

    /*
    async onActionClick(action, row) {
        switch (action.name) {
            case 'action1': await this.onAction1(row); return true;
        }
        return false;
    }
    */

}
<%=page%><%=form%>Controller;
