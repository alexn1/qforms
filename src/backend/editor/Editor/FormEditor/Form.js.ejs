class <%=page%><%=form%>Controller extends <%=_class%>Controller {

    /*
    constructor(...args) {
        super(...args);
    }
    */

    /*
    init() {
        super.init();
    }
    */

    /*
    deinit() {
        super.deinit();
    }
    */

    /*
    getViewClass() {
        return <%=page%><%=form%>View;
    }
    */

    /*
    async onAction1(row) {
    }
    async onActionClick(data, row) {
        switch (Model.getName(data)) {
            case 'action1': await this.onAction1(row); return true;
        }
        return false;
    }
    */

}
