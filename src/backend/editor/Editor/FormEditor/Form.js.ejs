class <%=page%><%=form%>FormController extends <%=_class%>Controller {

    /*
    constructor(...args) {
        super(...args);
    }
    */

    /*
    init() {
        super.init();
        this.getField('field1').on('change', this.onField1Change);
    }
    */

    /*
    deinit() {
        this.getField('field1').off('change', this.onField1Change);
        super.deinit();
    }
    */

    /*
    getViewClass() {
        return <%=page%><%=form%>FormView;
    }
    */

    /*
    async onActionClick(name, row) {
        switch (name) {
            case 'action1': await this.onAction1(row); return true;
        }
        return false;
    }
    
    async onAction1(row) {
    }
    */

    /*
    isActionEnabled(name) {
        // for table form
        // return this.isRowSelected();
        // return this.isViewMode();

        // for row form
        // if (name === 'actionName') return true;
        // return super.isActionEnabled(name);
    }
    */

    /*
    onField1Change = async e => {
        // this.getField('field1').onChange('newValue');
    }
    */

}
