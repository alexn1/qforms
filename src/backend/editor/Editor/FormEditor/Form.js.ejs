class <%=page%><%=form%>FormController extends <%=_class%>Controller {

    /*
    constructor(...args) {
        super(...args);
    }
    */

    /*
    init() {
        super.init();
        this.getField('field1').on('change', this.onField1Change);
    }
    */

    /*
    deinit() {
        this.getField('field1').off('change', this.onField1Change);
        super.deinit();
    }
    */

    /*
    getViewClass() {
        return <%=page%><%=form%><%=_class%>View;
    }
    */

    /*
    async onAction1(row) {
    }
    async onActionClick(name, row) {
        switch (name) {
            case 'action1': await this.onAction1(row); return true;
        }
        return false;
    }
    */

    /*
    onField1Change = async e => {
        this.getField('field2').onChange('newValue');
    }
    */

}
