class <%=page%><%=form%><%=field%>FieldController extends <%=formClass%><%=_class%>Controller {

    /*
    constructor(...args) {
        super(...args);
    }
    */

    /*
    init() {
        super.init();
    }
    */

    /*
    deinit() {
        super.deinit();
    }
    */

    /*
    renderViewStyle(row) {
        const value = this.model.getValue(row);
        let color = null;
        if (value === 'abc') {
            color = 'green';
        }
        return {color};
    }
    */
}
