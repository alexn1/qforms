class <%=page%><%=form%><%=field%>Controller extends <%=_class%>Controller {

    constructor(...args) {
        super(...args);
    }

    init() {
        super.init();
        //this.on('click', this.listeners.click = this.onClick.bind(this));
    }

    deinit() {
        //this.off('click', this.listeners.click);
        super.deinit();
    }

    /*
    onClick(e) {
        this.model.parent.openPage({
            name: 'PageName',
            key : JSON.stringify([e.row.<%=field%>])
        });
    }
    */

    /*
    setViewStyle(view, row) {
        //const bodyCell = view.parentNode;               // td element
        //const bodyRow  = bodyCell.parentNode;           // tr element
        //if (row.<%=field%> === 'value') {
        //    $(view).css('color', 'green');
        //} else {
        //    $(view).css('color', '');
        //}
    }
    */
}
<%=page%><%=form%><%=field%>Controller;
