class <%=page%><%=form%><%=field%>FieldView extends <%=formClass%><%=_class%>View {
    /*
    renderViewStyle(row) {
        const value = this.props.ctrl.getModel().getValue(row);
        let color = null;
        if (value === 'abc') {
            color = 'green';
        }
        return {color};
        }
    */
}
