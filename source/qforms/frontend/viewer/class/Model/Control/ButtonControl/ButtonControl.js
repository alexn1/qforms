'use strict';

QForms.inherit(ButtonControl, Control);

////////////////////////////////////////////////////////////////////////////////////////////////////
function ButtonControl(data, form) {
    Control.call(this, data, form);
}