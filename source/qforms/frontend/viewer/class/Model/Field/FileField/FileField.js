'use strict';

QForms.inherit(FileField, Field);

////////////////////////////////////////////////////////////////////////////////////////////////////
function FileField(name, form, data) {
    FileField.super_.call(this, name, form, data);
}