class <%=page%><%=form%>FormController extends <%=_class%>Controller {

    /*
    constructor(...args) {
        super(...args);
    }
    */

    /*
    init() {
        super.init();
        this.getField('field').on('change', this.onFieldChange);
    }
    */

    /*
    deinit() {
        this.getField('field').off('change', this.onFieldChange);
        super.deinit();
    }
    */

    /*
    getViewClass() {
        return <%=page%><%=form%>FormView;
    }
    */

    /*
    async onAction1(row) {
    }
    async onActionClick(name, row) {
        switch (name) {
            case 'action1': await this.onAction1(row); return true;
        }
        return false;
    }
    */

    /*
    onFieldChange = async e => {
        // this.getField('field').onChange('newValue');
    }
    */

}
