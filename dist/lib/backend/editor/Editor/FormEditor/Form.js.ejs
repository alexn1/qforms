class <%=page%><%=form%>FormController extends <%=_class%>Controller {

    /*
    constructor(...args) {
        super(...args);
    }
    */

    /*
    init() {
        super.init();
        this.getField('field').on('change', this.onAnyFieldChange);
    }
    */

    /*
    deinit() {
        this.getField('field').off('change', this.onAnyFieldChange);
        super.deinit();
    }
    */

    /*
    getViewClass() {
        return <%=page%><%=form%>FormView;
    }
    */

    /*
    async onActionClick(name, row) {
        switch (name) {
            case 'action1': await this.onAction1(row); return true;
        }
        return false;
    }
    async onAction1(row) {
    }
    */

    /*
    onAnyFieldChange = async e => {
        // this.getField('field').onChange('newValue');
    }
    */

}
