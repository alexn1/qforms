class <%=page%><%=_class%>View extends <%=_class%>View {
    /*
    renderActionIcon(action) {
        if (action === 'actionName') {
            return <NameIcon/>;
        }
        return <div style={{width: 24, height: 24}}/>;
    }
    */

    /*
    getStyle() {
        return {width: 640, height: 480};
    }
    */

    /*
    componentDidMount() {
        if (this.getCtrl().getModel().getKey()) {
            super.componentDidMount();
        }
        // auto focus to field in creation mode
    }
    */
}
